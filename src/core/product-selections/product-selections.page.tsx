import { PageWrapper } from '../../lib/common/page-wrapper';
import { ProductSelectionsContainer } from './product-selections.container';

export function ProductSelectionsPage() {
  return (
    <PageWrapper>
      <ProductSelectionsContainer />
    </PageWrapper>
  );
}
