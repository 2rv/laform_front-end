import { PageWrapper } from '../../lib/common/page-wrapper';
import { UnsubscribeNotificationContainer } from './unsubscribe-notification.container';

export function UnsubscribeNotificationPage() {
  return (
    <PageWrapper>
      <UnsubscribeNotificationContainer />
    </PageWrapper>
  );
}
