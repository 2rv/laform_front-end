import { PageWrapper } from '../../lib/common/page-wrapper';
import { SliderListContainer } from './slider-list.container';

export function SliderListPage() {
  return (
    <PageWrapper>
      <SliderListContainer />
    </PageWrapper>
  );
}
