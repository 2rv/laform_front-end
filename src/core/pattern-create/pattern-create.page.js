import { PageWrapper } from '../../lib/common/page-wrapper';
import { CreatePatternContainer } from './pattern-create.container';

export function CreatePatternPage() {
  return (
    <PageWrapper>
      <CreatePatternContainer />
    </PageWrapper>
  );
}
