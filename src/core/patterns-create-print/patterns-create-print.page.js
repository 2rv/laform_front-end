import { PageWrapper } from '../../lib/common/page-wrapper';
import { CreatePrintPatternContainer } from './patterns-create-print.container';

export function CreatePrintPatternPage() {
  return (
    <PageWrapper>
      <CreatePrintPatternContainer />
    </PageWrapper>
  );
}
